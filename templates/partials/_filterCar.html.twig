{{ form_start(form, {'class' : 'filter'}) }}

{{ form_row(form.car) }}

<h4>Catégories</h4>
{{ form_row(form.category) }}

<h4>Prix</h4>
<div class="row">
    <div class="col-6">
        {{ form_row(form.min) }}
    </div>
    <div class="col">
        {{ form_row(form.max) }}
    </div>
</div>
    <pre>
        {{ dump(min,max) }}
    </pre>
<button type="submit" class="btn btn-primary w-100 my-4">Filter</button>
    <div id="price-slider" data-min="{{min}}" data-max="{{max}}">
    </div>

{{ form_end(form) }}