<header class="header_visiteur">
    <section class="member">
        {% if app.user %}
            <a href="{{ path('account_logout')}}">
                <h2>Déconnexion </h2>
            </a>	
        {% else %}
            <a href="{{ path('account_login') }}">
                <h2>Espace membre</h2>
            </a>      
        {% endif %}
            <a href="{{ path('cars_home') }}">
                <img src="{{asset('build/images/logo.jpg')}}" alt="logo">
            </a>
    </section>	
    <section class="nav">
        <nav> 
            {# {% for category in categories %}
            <a href="{{ path('cars_category', {'id': category.id}) }}">{{category.name}}</a>
            {% endfor %}                 #}
            <a href="{{ path('cars_category', {'id': 211}) }}">Berline</a>
            <a href="{{ path('cars_category', {'id': 214}) }}">Cabriolet</a>
            <a href="{{ path('cars_category', {'id': 213}) }}">SUV</a>
            <a href="{{ path('cars_category', {'id': 212}) }}">4X4</a>  
            {% if app.user %}
                <a href="{{ path('account_user')}}">Mon Espace</a>
            {% endif %}            
        </nav>	
    </section>
</header>